# Starburst Galaxy (Trino over HTTPS)
STARBURST_SERVER=https://philtrail1.galaxy.starburst.io/
STARBURST_USER=larej93324@isfew.com/accountadmin
STARBURST_PASSWORD=your-BustConsulting2026
STARBURST_CATALOG=kaggle_tx_data
STARBURST_SCHEMA=discovered_schema
# Optional: limit information_schema metadata to one table
STARBURST_TABLE=kaggle_tx_data

# S3 inventory (same data lake backing the Galaxy catalog)
AWS_REGION=us-east-1
S3_BUCKET=kaggle-tx-data
# Optional key prefix
S3_PREFIX=

# Caps / tuning
MAX_METADATA_ROWS=800
MAX_S3_OBJECTS=2500
PORT=8787